/*const a = document.querySelector(".card-text")
let trimString = function(string, length) {
  return string.length > length ? string.substring(0, length) + '...' : string;
};
a.innerHTML = trimString(a.innerHTML, 94)*/